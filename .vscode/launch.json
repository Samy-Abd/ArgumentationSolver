{
    // Use the debugger window in vscode along with this file to execute the program in debug mode
    // while providing it the arguments below.
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": true,
            "args": ["-p", "DS_ST", "-f", "tests/test_af1.apx", "-a", "B"] //Define the arguments here
        }
    ]
}